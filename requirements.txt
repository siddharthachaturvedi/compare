Flask==2.2.2  # A more recent version, check Flask's documentation for the latest stable release
Flask-CORS==3.0.10
requests==2.26.0
gunicorn==20.1.0
Werkzeug==2.0.1  # Updated to a compatible version with the newer Flask
